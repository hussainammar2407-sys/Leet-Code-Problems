#include <cmath>

class Solution {
public:
    bool isPowerOfThree(int n) {
        if (n <= 0) return false;
        double a = log(n) / log(3);
        return abs(a - round(a)) < 1e-10;  // Check if a is close to an integer
    }
};
